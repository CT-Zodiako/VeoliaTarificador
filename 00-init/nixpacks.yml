# nixpacks.yml
setup:
  # Si usas nodejs, npm, o yarn puedes configurar aquí la versión
  nodejs: "18.x"

install:
  command: npm ci --legacy-peer-deps

build:
  command: npm run build

start:
  command: npm run start:prod

root: ./00-init

cache:
  - /root/.npm
